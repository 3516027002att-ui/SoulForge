/**
 * AI tools public surface.
 *
 * Production consumers should use packages/core/src/ai/toolRegistry.ts
 * (createDefaultToolRegistry). The scaffold registry was removed after architecture
 * smoke migrated to the production ToolRegistry.
 */
export * from './policyGate.js';
