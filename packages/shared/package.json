{
  "name": "@soulforge/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:editor-catalog-schema": "tsc -p tsconfig.json && node dist/editor-catalog.test.js",
    "test:flver-pages": "tsc -p tsconfig.json && node dist/flver-editor.test.js",
    "test:tpf-pages": "tsc -p tsconfig.json && node dist/tpf-editor.test.js"
  }
}
