name: runtime-operation-workspace-fix

on:
  pull_request:
    branches: [main, master]
    paths:
      - scripts/apply-runtime-operation-workspace-fix.mjs
      - .github/workflows/runtime-operation-workspace-fix.yml

permissions:
  contents: write

concurrency:
  group: runtime-operation-workspace-fix
  cancel-in-progress: true

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout runtime branch
        uses: actions/checkout@v4
        with:
          ref: agent/me3-runtime-foundation
          fetch-depth: 0
      - name: Apply workspace identity fix
        run: node scripts/apply-runtime-operation-workspace-fix.mjs
      - name: Remove one-shot migration
        run: |
          rm scripts/apply-runtime-operation-workspace-fix.mjs
          rm .github/workflows/runtime-operation-workspace-fix.yml
      - name: Commit workspace identity fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(runtime): preserve workspace in operation verification"
          git push origin HEAD:agent/me3-runtime-foundation
