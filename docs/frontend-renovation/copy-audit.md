# 微文案审查（de-slop catalog 参照）

范围：renderer 全部可见文案。分类：保留 / 修改 / 删除 / 需产品确认 / 内部术语转详情。
原则：描述对象、状态、已发生动作、下一步、范围、可逆性、失败原因；禁无证据形容词与名词串联。

## 删除（本轮及前两轮已落地）

| 原文案 | 问题 | 处置 |
|---|---|---|
| 魂游 Mod 工作台 | slogan tag | 删除，换工作区·游戏 |
| V0.5 · 证据驱动写路径（pill） | 版本宣传胶囊 | 删除 |
| 五节点流程卡文案（资源证据/Typed Mutation/…） | 教程式数字圆圈+内部名 | 删除，换真实摘要 |
| 你好，我是 SoulForge Agent…告诉我修改目标 | 聊天机器人自我介绍 | 删除，换空任务态 |
| gpt-5.2 · 证据驱动 | 编造具体性 | 删除，换真实状态 |
| 通过 Patch Engine 提交 | 内部引擎名作主动词 | 改"写入补丁" |
| 继续工作 | 泛化标题 | 改"最近打开" |
| renderer 不暴露字节级写路径 | 开发者语言 | 改"该资源当前版本不提供写入入口" |

## 内部术语转详情（保留但降级）

| 位置 | 主文案 | 详情行 |
|---|---|---|
| 设置·写入路径 | 变更必须经暂存与备份后写入 | （内部：PatchIR / Patch Engine） |
| 设置·回滚 | 写入前自动备份，可按操作、文件或资源条目回滚 | — |
| 审查卡 | 目标/字段/原值→新值/证据/影响范围/可逆性 | 内部：typed mutation → PatchIR |
| 错误面板 | 资源解析失败 + 原因 + 未做任何修改 | diagnostic: CODE · revision |

## 保留（符合原则）

- 状态栏：当前：{file}（只读）/ 备份：启用 · 可回滚 / {N} 资源已索引。
- 暂存：写入前自动备份原文件，写入后可回滚。/ 放弃全部（N 项）/ 写入中…。
- 失败：写入失败：暂存后资源被外部修改。暂存已保留，未修改任何文件，请重读证据后重试。
- Agent：目标/证据/日志/待审批/失败 · 校验冲突；批准并入暂存 / 拒绝；已入暂存区，等待写入 / 已拒绝，未产生任何写入。
- 空态：暂存区为空。变更经批准后进入暂存，验证后写入。/ 暂无最近打开。从左侧资源树选择资源，或按 Ctrl K 搜索。/ 没有进行中的任务…。
- 表格行编辑标记"改"（颜色+文字双通道）。

## 需产品确认（记录，不阻塞）

1. 状态栏"1,284 资源已索引"为 mock；接线后必须来自 VFS 索引真实计数，且与树计数同源。
2. "审计与回滚"面板名是否对终端用户过技术；候选"写入历史"。暂保留（与交接书审计口径一致）。
3. 审查卡"影响范围 1 条记录 · 1 个字段"接线后应由 PatchIR 真实计算。
