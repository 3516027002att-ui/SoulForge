# SF-08执行卡：FMG 文本身份、编码和同容器多表写入

基准为`f9cb0cd76a11bfa2857b48acbc779bfac0355d96`。本卡是施工任务，当前状态not_started。先读`chapters/00-执行总则.md`与本卡；不可只读标题。依赖：SF-06, SF-07, SF-12。依赖指已合并的接口与必要验证，不是另一个模型说“做了”。

## 允许修改的位置

既有源文件候选白名单如下，必须在SF-00的baseline-map中证明实际存在与锚点一致；不存在不按同名自建。
- `bridge/SoulForge.Bridge/FmgNativeDocument.cs`
- `bridge/SoulForge.Bridge/FmgNativeWriter.cs`
- `packages/core/src/editing/fmgEdit.ts`
- `packages/core/src/editing/fmgBridgeCommit.ts`

新增对象如下；若同功能已有实现，只扩展既有owner，不创建第二套。同功能复用决策写入baseline-map，由集成者固定实际路径。
- `packages/core/src/testing/runAuditSf08Smoke.ts`（新增）

共同登记文件仅由集成者修改`package.json`、`packages/core/package.json`、`scripts/verify/tiers.mjs`。允许本任务添加自己的测试helper与fixture描述到`packages/core/src/testing/audit/sf-08/`；这是新增测试目录，不可在其中复制一套生产算法并以此替代生产测试。修改名单之外的native/host入口必须先加入baseline-map得到精确路径，不默认授权整个目录。

需要取证绑定：本卡列出的固定入口。绑定记录包括实际文件、symbol、调用者和版本；不以猜测文件名代替。renderer实现不在本卡权限内。

## 算法与业务步骤

## SF-08：FMG 文本身份、编码和同容器多表写入

**依据。** 报告 §5.2；S20；T19～T21。现有编码和 sibling 检查保留。先补验，不声称原报告已经证实所有下列情况有 bug。

**入口。** `FmgNativeDocument.cs` / `FmgNativeWriter.cs`，Core `fmgEdit.ts` / `fmgBridgeCommit.ts`，`saveContainerChild.ts`，工具 `read_fmg_entries` / `mutate_fmg_entries`。定位 writer 的 `ReadPatches`、`VerifyMutations`、`VerifySiblingPreservation` 和容器 `entryIndex` 选择。

`TextEntryHandle` 固定到 workspace、outer、childIndex+expectedChildHash、language、category、slotIndex、expectedTextId、sourceVersion。语言/category 是结构化 namespace，不从用户中文名称猜。重复 textId 时只有支持物理槽的 writer 才能修改；否则明确拒绝该目标，不任取第一项。

**模拟算法。** 在不可变 FMG entry 槽序列上绑定目标；构建工作序列并按顺序应用 insert/update/delete；同目标多次更新采用最后状态；建立最终 slots + null/empty 标记 + group 期望。编码前检查字符串不能包含 U+0000、孤立 UTF-16 surrogate。不要 normalize、trim 或改变换行，除非用户计划显式包含该文本变化。

重建复用现有 FMG/BND writer；验证最终有序槽与未触及 sibling。null 指针文本与空 UTF-16 字符串是两种状态，writer 不支持其中一种时返回不支持。新增/删除改变 group 范围时用现有 group 重建代码并由独立 reader 检查，禁止手改几处偏移凑结果。

同一 msgbnd 内多张表的修改必须先分别产生 child 候选，再由 SF-12 对同一个 outer 聚合一次替换。不能每个工具并发拿原容器生成一个全量输出，再按完成顺序覆盖。顺序提交虽然可能避免丢写，也不代表复合用户任务有完整 ChangeSet；需要按用户确认的范围组织事务。

**测试。** 同 textId 出现在两个语言/分类，修改只触及目标；在两个 child 同时修改，各自 sibling 保留；相同文本 ID 重复槽；null/空字符串；中文、日文、emoji、换行；U+0000/孤立代理项拒绝；同目标两次写；修改后以 transactionId 回滚；用户表格显示与 native readback 一致。SF-00 必须记录这些测试真实执行。

## 测试入口与登记

新增薄入口`packages/core/src/testing/runAuditSf08Smoke.ts`。它只按显式`--layer unit|native`调用本任务的真实测试，不是另一个测试框架。unit使用确定fixture调用生产导出；native通过现有`resolveNativeFixture`、`createSmokeWorkspace`和Bridge测试facade调用真实native命令。未知layer退出非零；缺native资源输出skipped，由require-executed判失败。章节提及的具名smoke可作为本入口调用的专项实现，不能再复制同一算法。

core新增脚本（需实施后才存在）：
```json
{
  "test:audit-sf-08-unit": "tsc -b ../shared . && node dist/testing/runAuditSf08Smoke.js --layer unit",
  "test:audit-sf-08-native": "tsc -b ../shared . && node dist/testing/runAuditSf08Smoke.js --layer native"
}
```

根package用同名脚本转发至`@soulforge/core`，tier按脚本后缀登记unit/native。新入口不得用 `assert(true)`、只读源码includes或只跑reference程序。注册后先list，确认包含`tasks/SF-08.required-suites.json`列出的全部项，再执行：
```bash
node scripts/verify.mjs --audit
node scripts/verify.mjs --tier all --filter audit-sf-08 --list
node scripts/verify.mjs --tier all --filter audit-sf-08 --require-executed --no-bail --json-out artifacts/audit-execution/SF-08.json
node scripts/audit-execution/check-verify-summary.mjs artifacts/audit-execution/SF-08.json docs/audit-execution/tasks/SF-08.required-suites.json
npm run typecheck
```

`tools/check-verify-summary.mjs`在SF-00复制/集成到上述新增scripts路径；required-suites文件随执行资料放到上述docs路径。不得在文件还不存在时声称命令可直接运行。

现有core回归另外执行并保留结果；它们不替代新增反例：
```bash
npm run test:native-fmg -w @soulforge/core
npm run test:fmg-reference-integrity -w @soulforge/core
```

## 通过条件与禁止捷径

关联原报告问题：F07。关联验收项：T19, T20, T21。每个T项的原始判据见总表，不能只检查返回ok。

必须记录旧反例的失败、修复后的通过、至少一个无关正常行为不回退以及至少一个非法输入仍拒绝。实际仓库入口缺失、环境缺失、依赖未合并、未知native profile、前端未接线各自标blocker；不能删测试或降级类型消除红色。参考算法通过不等于本任务完成。

输出 `artifacts/audit-execution/SF-08/task-evidence.json`，包含实际commit、changedFiles、productionSymbols、测试结果/哈希、受影响schema、剩余限制。没有运行的层用not_run，不能填写passed。
