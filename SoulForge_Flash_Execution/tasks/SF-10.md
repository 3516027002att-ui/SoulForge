# SF-10执行卡：Lua/LUABND 源码、字节码与混合编码

基准为`f9cb0cd76a11bfa2857b48acbc779bfac0355d96`。本卡是施工任务，当前状态not_started。先读`chapters/00-执行总则.md`与本卡；不可只读标题。依赖：SF-07, SF-12。依赖指已合并的接口与必要验证，不是另一个模型说“做了”。

## 允许修改的位置

既有源文件候选白名单如下，必须在SF-00的baseline-map中证明实际存在与锚点一致；不存在不按同名自建。
- `packages/core/src/script/scriptSourceWriteback.ts`
- `packages/core/src/script/plaintextScriptEntry.ts`
- `packages/core/src/editing/luabndEdit.ts`

新增对象如下；若同功能已有实现，只扩展既有owner，不创建第二套。同功能复用决策写入baseline-map，由集成者固定实际路径。
- `packages/core/src/script/scriptLoaderProfile.ts`（新增）
- `packages/core/src/testing/runAuditSf10Smoke.ts`（新增）

共同登记文件仅由集成者修改`package.json`、`packages/core/package.json`、`scripts/verify/tiers.mjs`。允许本任务添加自己的测试helper与fixture描述到`packages/core/src/testing/audit/sf-10/`；这是新增测试目录，不可在其中复制一套生产算法并以此替代生产测试。修改名单之外的native/host入口必须先加入baseline-map得到精确路径，不默认授权整个目录。

需要取证绑定：本卡列出的固定入口。绑定记录包括实际文件、symbol、调用者和版本；不以猜测文件名代替。renderer实现不在本卡权限内。

## 算法与业务步骤

## SF-10：Lua/LUABND 源码、字节码与混合编码

**依据。** 报告 §5.4；S22；T25/T26。原有 `encodeScriptSourceForWriteback` 识别 bytecode 后编码 UTF-8；它证明的是转换，不是游戏加载支持。

**入口。** `scriptSourceWriteback.ts`、`plaintextScriptEntry.ts`、`plaintextScriptEdit.ts`、`editing/luabndEdit.ts`、`scriptContainerEvidence.ts`、`dsLuaDecompilerLocator.ts`、Bridge 的 LUABND writer 路径。不得仅在提示词中禁止危险转换。

新增 `ScriptLoaderProfile`：game/version、脚本资源类别、容器/条目模式、允许 representation、编码、匹配的语法验证工具、反编译工具版本、已验样本。profile 由签名/登记源选择，模型不传任意 executable/argv。只有经过验证允许 plaintext 的 bytecode 资源位置，才开放 bytecode→source；其他保留 read-only 或整文件替换的受限 capability，不把其广告成源码编辑。

**读流程。** 提取 child bytes 和 hash；区分 plaintext/bytecode/unknown；按实际检测编码显示；bytecode 反编译输出标注 derivedSource、decompilerVersion、warnings 和能否写回。反编译失败、占位文本、部分函数丢失都不能获得完整源码编辑标记。

**明文修改算法。** 绑定完整 sourceVersion 与选区；若模型只有摘要，先读取完整目标范围和保留边界；把编辑表示为不相交的原文 range patch。范围含 start/end 和 expectedSliceHash；先验证所有 range，再按起点逆序应用，避免前面的长度变化移动后面的位置。重叠 patch 默认拒绝，不能依赖任意覆盖顺序。

**混合编码算法。** 当前限制“只改纯 ASCII 行、行数不变”保留。按原始 bytes 扫描换行建立 line span，不通过解码再编码整篇。对每个改动行验证原 bytes 和新字符串全部 ASCII，换行序列保留；未改 span 直接复制。长度改变时预计算最终字节数，分配一次 output，然后顺序拷贝源 span 与新 bytes。保留原 trailing padding。复杂度 O(B+E)，E 是改动文本大小；禁止 `[...content]` 创建每字节 JS number 列表去过滤高字节。

全 Unicode 编码路径用现有 encodePlaintext，保留 BOM、换行和原有编码策略。无法编码的新字符直接失败；禁止静默改成 UTF-8。字节码转换需先经过 loaderProfile，再执行对应语法/入口验证，然后经 LUABND/BND 重建和统一事务提交。

**验证层。** 编码可逆；语法检查；必要入口/导出仍存在；native container 重读；游戏加载/执行。各层单独记录。缺对应 Lua 版本工具时不能调用系统随便一个 `lua`/`luac` 代替并报告兼容。原报告没有固定每类脚本的语言版本，本方案不猜版本号。

**测试。** ASCII 行长度变化但非 ASCII span 逐字节不变；CRLF/LF/末尾无换行；BOM/尾 padding；混合编码改非 ASCII 拒绝；增删行拒绝；重叠 range 拒绝；旧 slice hash；bytecode 未登记 profile 拒绝；错误反编译输出不提交；syntax validator exit非0/缺工具阻止对应写路径；写入后 child/outer哈希和 sibling正确。

## 测试入口与登记

新增薄入口`packages/core/src/testing/runAuditSf10Smoke.ts`。它只按显式`--layer unit|native`调用本任务的真实测试，不是另一个测试框架。unit使用确定fixture调用生产导出；native通过现有`resolveNativeFixture`、`createSmokeWorkspace`和Bridge测试facade调用真实native命令。未知layer退出非零；缺native资源输出skipped，由require-executed判失败。章节提及的具名smoke可作为本入口调用的专项实现，不能再复制同一算法。

core新增脚本（需实施后才存在）：
```json
{
  "test:audit-sf-10-unit": "tsc -b ../shared . && node dist/testing/runAuditSf10Smoke.js --layer unit",
  "test:audit-sf-10-native": "tsc -b ../shared . && node dist/testing/runAuditSf10Smoke.js --layer native"
}
```

根package用同名脚本转发至`@soulforge/core`，tier按脚本后缀登记unit/native。新入口不得用 `assert(true)`、只读源码includes或只跑reference程序。注册后先list，确认包含`tasks/SF-10.required-suites.json`列出的全部项，再执行：
```bash
node scripts/verify.mjs --audit
node scripts/verify.mjs --tier all --filter audit-sf-10 --list
node scripts/verify.mjs --tier all --filter audit-sf-10 --require-executed --no-bail --json-out artifacts/audit-execution/SF-10.json
node scripts/audit-execution/check-verify-summary.mjs artifacts/audit-execution/SF-10.json docs/audit-execution/tasks/SF-10.required-suites.json
npm run typecheck
```

`tools/check-verify-summary.mjs`在SF-00复制/集成到上述新增scripts路径；required-suites文件随执行资料放到上述docs路径。不得在文件还不存在时声称命令可直接运行。

现有core回归另外执行并保留结果；它们不替代新增反例：
```bash
npm run test:plaintext-script-edit -w @soulforge/core
npm run test:script-container-load-preflight -w @soulforge/core
```

## 通过条件与禁止捷径

关联原报告问题：原报告架构建议/补验范围，未新增确定bug断言。关联验收项：T25, T26。每个T项的原始判据见总表，不能只检查返回ok。

必须记录旧反例的失败、修复后的通过、至少一个无关正常行为不回退以及至少一个非法输入仍拒绝。实际仓库入口缺失、环境缺失、依赖未合并、未知native profile、前端未接线各自标blocker；不能删测试或降级类型消除红色。参考算法通过不等于本任务完成。

输出 `artifacts/audit-execution/SF-10/task-evidence.json`，包含实际commit、changedFiles、productionSymbols、测试结果/哈希、受影响schema、剩余限制。没有运行的层用not_run，不能填写passed。native profile required; unsupported sub-capabilities remain gated
