# SF-03执行卡：Region 变换模型与 shape 尺寸

基准为`f9cb0cd76a11bfa2857b48acbc779bfac0355d96`。本卡是施工任务，当前状态not_started。先读`chapters/00-执行总则.md`与本卡；不可只读标题。依赖：SF-01, SF-02。依赖指已合并的接口与必要验证，不是另一个模型说“做了”。

## 允许修改的位置

既有源文件候选白名单如下，必须在SF-00的baseline-map中证明实际存在与锚点一致；不存在不按同名自建。
- `bridge/SoulForge.Bridge/MsbNativeDocument.cs`
- `bridge/SoulForge.Bridge/MsbNativeWriter.cs`
- `packages/shared/src/map-document.ts`
- `packages/core/src/editing/mapService.ts`
- `packages/core/src/ai/toolRegistry.ts`

新增对象如下；若同功能已有实现，只扩展既有owner，不创建第二套。同功能复用决策写入baseline-map，由集成者固定实际路径。
- `packages/shared/src/msb-shape-profile.ts`（新增）
- `packages/core/src/testing/runAuditSf03Smoke.ts`（新增）

共同登记文件仅由集成者修改`package.json`、`packages/core/package.json`、`scripts/verify/tiers.mjs`。允许本任务添加自己的测试helper与fixture描述到`packages/core/src/testing/audit/sf-03/`；这是新增测试目录，不可在其中复制一套生产算法并以此替代生产测试。修改名单之外的native/host入口必须先加入baseline-map得到精确路径，不默认授权整个目录。

需要取证绑定：本卡列出的固定入口。绑定记录包括实际文件、symbol、调用者和版本；不以猜测文件名代替。renderer实现不在本卡权限内。

## 算法与业务步骤

## SF-03：Region 变换模型与 shape 尺寸

**报告依据。** F02、T03/T04。当前 `+0x30/+0x34/+0x38` 属于数据指针，必须零写入。

**修改范围。** `MsbNativeDocument.cs`、`MsbNativeWriter.cs`、shared `map-document.ts`、Core `msbBridgeRead.ts` / `msbBridgeCommit.ts`、`mapService.ts`，以及 shared capability 投影。新类型建议为 `MapRegionTransform={position,rotation}` 与 `MapRegionShape`。Part 继续使用 position/rotation/scale。

`MapRegionShape` 是判别联合，初始包含 `kind:'unsupported'` 与 `kind:'point'` 的只读表示；Sphere/Box/Cylinder 的可写分支必须有 profile。profile 至少包含 shapeType 的原生判别值、shapeData 指针规则、字段类型/偏移/单位、允许零值、未触及字节规则、独立依据 hash、已验证样本集合。原报告没有给出完整 shape 字段表，本方案不伪造数字偏移。

**尺寸运算裁定。** 已经验证的 Sphere 仅接受统一缩放，半径 r'=r*s；非均匀缩放不默默平均。已验证 Box 的各原生尺寸依轴语义乘对应比例；Blender 轴映射经 SF-25 的坐标合同，不直接把 scaleX 当 width。已验证 Cylinder 的两个径向比例相等才能仍表示圆柱，否则拒绝；高度独立变化。Point 没有尺寸字段。所有 shape 变换必须产生明确的 `set_region_shape` 命令，禁止写 `set_region_transform.scale`。

输入尺寸必须有限且符合 profile 的正数/非负规则；未知类型、未知布局或无法表示的形变返回 `SHAPE_OPERATION_UNSUPPORTED`。shape 类型变更不是尺寸更新，初期不开放，因为它可能改变 payload 长度及引用。

**执行门槛。** SF-03 的第一阶段交付为 reader/DTO 分离与通用 scale 拒绝；第二阶段逐 profile 开放 shape 尺寸。缺少独立布局的形状只读。第二阶段不能因第一阶段通过而被标为完成。

**测试。** 保存 Region +0x30/+0x38 原始八字节指针，任何位置/旋转或已支持 shape 尺寸修改后都必须不变。恶意 scale=1/null/0 均拒绝。支持类型从独立 reader 读取新尺寸，未知类型零写入。盒子采用非对称尺寸以发现轴交换；球采用非均匀输入以发现非法近似。

## 测试入口与登记

新增薄入口`packages/core/src/testing/runAuditSf03Smoke.ts`。它只按显式`--layer unit|native`调用本任务的真实测试，不是另一个测试框架。unit使用确定fixture调用生产导出；native通过现有`resolveNativeFixture`、`createSmokeWorkspace`和Bridge测试facade调用真实native命令。未知layer退出非零；缺native资源输出skipped，由require-executed判失败。章节提及的具名smoke可作为本入口调用的专项实现，不能再复制同一算法。

core新增脚本（需实施后才存在）：
```json
{
  "test:audit-sf-03-unit": "tsc -b ../shared . && node dist/testing/runAuditSf03Smoke.js --layer unit",
  "test:audit-sf-03-native": "tsc -b ../shared . && node dist/testing/runAuditSf03Smoke.js --layer native"
}
```

根package用同名脚本转发至`@soulforge/core`，tier按脚本后缀登记unit/native。新入口不得用 `assert(true)`、只读源码includes或只跑reference程序。注册后先list，确认包含`tasks/SF-03.required-suites.json`列出的全部项，再执行：
```bash
node scripts/verify.mjs --audit
node scripts/verify.mjs --tier all --filter audit-sf-03 --list
node scripts/verify.mjs --tier all --filter audit-sf-03 --require-executed --no-bail --json-out artifacts/audit-execution/SF-03.json
node scripts/audit-execution/check-verify-summary.mjs artifacts/audit-execution/SF-03.json docs/audit-execution/tasks/SF-03.required-suites.json
npm run typecheck
```

`tools/check-verify-summary.mjs`在SF-00复制/集成到上述新增scripts路径；required-suites文件随执行资料放到上述docs路径。不得在文件还不存在时声称命令可直接运行。

现有core回归另外执行并保留结果；它们不替代新增反例：
```bash
npm run test:native-msb-writer -w @soulforge/core
```

## 通过条件与禁止捷径

关联原报告问题：F02。关联验收项：T03, T04。每个T项的原始判据见总表，不能只检查返回ok。

必须记录旧反例的失败、修复后的通过、至少一个无关正常行为不回退以及至少一个非法输入仍拒绝。实际仓库入口缺失、环境缺失、依赖未合并、未知native profile、前端未接线各自标blocker；不能删测试或降级类型消除红色。参考算法通过不等于本任务完成。

输出 `artifacts/audit-execution/SF-03/task-evidence.json`，包含实际commit、changedFiles、productionSymbols、测试结果/哈希、受影响schema、剩余限制。没有运行的层用not_run，不能填写passed。native profile required; unsupported sub-capabilities remain gated
