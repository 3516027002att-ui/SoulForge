# SF-17执行卡：启动、PARAM与长文档热路径

基准为`f9cb0cd76a11bfa2857b48acbc779bfac0355d96`。本卡是施工任务，当前状态not_started。先读`chapters/00-执行总则.md`与本卡；不可只读标题。依赖：SF-07, SF-15, SF-16。依赖指已合并的接口与必要验证，不是另一个模型说“做了”。

## 允许修改的位置

既有源文件候选白名单如下，必须在SF-00的baseline-map中证明实际存在与锚点一致；不存在不按同名自建。
- `packages/core/src/workspace/scanWorkspace.ts`
- `packages/core/src/editing/editorDocumentStore.ts`
- `packages/core/src/bridge/runBridge.ts`
- `packages/shared/src/param-ipc-protocol.ts`

新增对象如下；若同功能已有实现，只扩展既有owner，不创建第二套。同功能复用决策写入baseline-map，由集成者固定实际路径。
- `packages/core/src/testing/runAuditSf17Smoke.ts`（新增）

共同登记文件仅由集成者修改`package.json`、`packages/core/package.json`、`scripts/verify/tiers.mjs`。允许本任务添加自己的测试helper与fixture描述到`packages/core/src/testing/audit/sf-17/`；这是新增测试目录，不可在其中复制一套生产算法并以此替代生产测试。修改名单之外的native/host入口必须先加入baseline-map得到精确路径，不默认授权整个目录。

需要取证绑定：desktop-main-resource-loading。绑定记录包括实际文件、symbol、调用者和版本；不以猜测文件名代替。renderer实现不在本卡权限内。

## 算法与业务步骤

## SF-17：启动、PARAM与长文档热路径

### 4.4.1 启动扫描

保留现有 `scanWorkspace` 的 `includeContentHashes`能力。桌面open路径必须显式false，catalog-ready只需要文件名/大小/mtime/格式候选，不做非必要全文SHA。深层hash/parse/index通过现有任务队列执行，可取消，且不阻塞目录展示。

mtime/size相同只能用于缓存候选，不能证明文件没变。打开为可写native文档、提交前置检查、外部文件变化确认仍需内容hash。首屏展示旧索引时标其revision/verification状态，不把预览缓存提升为写入证据。

`scanWorkspace`目录遍历可使用有界并发stat队列，初值16；hash独立队列初值2。不能对每个文件同时开启stream以追求峰值速度。目录进度事件按时间或批量合并，不每个字节触发React更新。

### 4.4.2 PARAM数据虚拟化

定位生产PARAM打开路径，记录renderer→preload→main→Bridge实际命令。已有session/index/readRows则复用，不另造loadAll2。目录/行名称轻量索引和row payload分开；首屏只请求可见窗口加固定overscan（初值上下各32行），每次payload最多200行且受字节上限约束。

行索引可分批传递，搜索在主进程索引或Bridge session里完成。不要为了“所有行能搜索”把每行完整raw bytes送进renderer。大metadata超过单包上限同样分页，不用名字是轻量信息作为无限包的理由。

选择一个字段/行时使用已绑定的RowHandle；合并相邻或重叠读取请求；同snapshot相同field集合single-flight；每次返回携带sourceVersion。用户快速切行A→B，A晚返回不能覆盖B；workspace/tab关闭取消订阅。writer修改后推送新版本的已验证行，失效旧cache，不全表reload。

### 4.4.3 EMEVD/脚本长文档

初次读取大纲/块索引，正文按事件或范围读取；编辑使用已有语言服务增量入口，不能每个键盘输入重新反编译全部脚本。请求带documentVersion与editSequence，只有最新版本诊断可呈现。过期结果丢弃并清理资源。

对必需整篇语法分析的路径，放worker/子进程并做debounce，但保存时必须完成当前版本检查；debounce不是跳过验证。AST缓存以正文hash+schema版本为键，不能只用文件名。

### 4.4.4 真实测量

新增trace字段：operation/parentTraceId、processId、command、resource identity、sourceVersion、local monotonic start/duration、readBytes、decompressCount、parseCount、projectionCount、wireBytes、queueMs、cancelState。不同进程的monotonic时钟不可直接相减，分别记录duration和父子关联；wall clock只作事件定位。

性能对比固定同资产、同build环境、cold/warm条件；每类至少30次样本报告p50/p95和峰值内存，不能挑最好一次。预算目标来自原报告但需实机校准。测试若禁掉验证得到更快，或改成空fixture得到低耗时，直接失败。

前端agent任务为真实消费分页、增量diagnostics和verifiedPostState。若生产renderer仍走旧loadAll，后端unit再绿也不算性能交付。SF-29的四场景负责最终证明。

## 测试入口与登记

新增薄入口`packages/core/src/testing/runAuditSf17Smoke.ts`。它只按显式`--layer unit|native`调用本任务的真实测试，不是另一个测试框架。unit使用确定fixture调用生产导出；native通过现有`resolveNativeFixture`、`createSmokeWorkspace`和Bridge测试facade调用真实native命令。未知layer退出非零；缺native资源输出skipped，由require-executed判失败。章节提及的具名smoke可作为本入口调用的专项实现，不能再复制同一算法。

core新增脚本（需实施后才存在）：
```json
{
  "test:audit-sf-17-unit": "tsc -b ../shared . && node dist/testing/runAuditSf17Smoke.js --layer unit",
  "test:audit-sf-17-native": "tsc -b ../shared . && node dist/testing/runAuditSf17Smoke.js --layer native"
}
```

根package用同名脚本转发至`@soulforge/core`，tier按脚本后缀登记unit/native。新入口不得用 `assert(true)`、只读源码includes或只跑reference程序。注册后先list，确认包含`tasks/SF-17.required-suites.json`列出的全部项，再执行：
```bash
node scripts/verify.mjs --audit
node scripts/verify.mjs --tier all --filter audit-sf-17 --list
node scripts/verify.mjs --tier all --filter audit-sf-17 --require-executed --no-bail --json-out artifacts/audit-execution/SF-17.json
node scripts/audit-execution/check-verify-summary.mjs artifacts/audit-execution/SF-17.json docs/audit-execution/tasks/SF-17.required-suites.json
npm run typecheck
```

`tools/check-verify-summary.mjs`在SF-00复制/集成到上述新增scripts路径；required-suites文件随执行资料放到上述docs路径。不得在文件还不存在时声称命令可直接运行。

现有core回归另外执行并保留结果；它们不替代新增反例：
```bash
npm run test:editor-bounded-access -w @soulforge/core
npm run test:performance-baseline -w @soulforge/core
```

## 通过条件与禁止捷径

关联原报告问题：原报告架构建议/补验范围，未新增确定bug断言。关联验收项：T24, T33, T34。每个T项的原始判据见总表，不能只检查返回ok。

必须记录旧反例的失败、修复后的通过、至少一个无关正常行为不回退以及至少一个非法输入仍拒绝。实际仓库入口缺失、环境缺失、依赖未合并、未知native profile、前端未接线各自标blocker；不能删测试或降级类型消除红色。参考算法通过不等于本任务完成。

输出 `artifacts/audit-execution/SF-17/task-evidence.json`，包含实际commit、changedFiles、productionSymbols、测试结果/哈希、受影响schema、剩余限制。没有运行的层用not_run，不能填写passed。
