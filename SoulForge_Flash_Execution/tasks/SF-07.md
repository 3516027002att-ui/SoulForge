# SF-07执行卡：所有原生工具的可读、可续读、可写合同

基准为`f9cb0cd76a11bfa2857b48acbc779bfac0355d96`。本卡是施工任务，当前状态not_started。先读`chapters/00-执行总则.md`与本卡；不可只读标题。依赖：SF-00。依赖指已合并的接口与必要验证，不是另一个模型说“做了”。

## 允许修改的位置

既有源文件候选白名单如下，必须在SF-00的baseline-map中证明实际存在与锚点一致；不存在不按同名自建。
- `packages/core/src/ai/agentToolBridge.ts`
- `packages/core/src/ai/toolRegistry.ts`
- `packages/core/src/editing/nativeEditSession.ts`

新增对象如下；若同功能已有实现，只扩展既有owner，不创建第二套。同功能复用决策写入baseline-map，由集成者固定实际路径。
- `packages/shared/src/native-evidence-contract.ts`（新增）
- `packages/core/src/testing/runAuditSf07Smoke.ts`（新增）

共同登记文件仅由集成者修改`package.json`、`packages/core/package.json`、`scripts/verify/tiers.mjs`。允许本任务添加自己的测试helper与fixture描述到`packages/core/src/testing/audit/sf-07/`；这是新增测试目录，不可在其中复制一套生产算法并以此替代生产测试。修改名单之外的native/host入口必须先加入baseline-map得到精确路径，不默认授权整个目录。

需要取证绑定：desktop-main-currentToolContext, desktop-main-confirmation。绑定记录包括实际文件、symbol、调用者和版本；不以猜测文件名代替。renderer实现不在本卡权限内。

## 算法与业务步骤

## SF-07：所有原生工具的可读、可续读、可写合同

**依据。** 报告 §5、§7.6、§8.7；S30/S31。这个任务是工具适配，不新建万能编辑器。

**范围。** `agentToolBridge.ts`、`toolRegistry.ts`、`nativeEditSession.ts`、现有各领域 read/mutate facade、shared 结果 envelope。新增 `packages/shared/src/native-evidence-contract.ts`，不与现有 envelope 平行维护第二套 data。

每次 native read 返回：object handle、source identity、version、可见字段、field capabilities、完整性、cursor。每个字段区分 `native_value`、`derived_value`、`external_metadata`、`display_label`。地图 Name/EntityID、TAE event param、FMG 文本、工具说明中的术语不能落入同一个编辑入口。

可写字段描述符包括 fieldId、nativeType、width、nullable、enumSchema、referenceNamespace、writable、blockedReason、requiredReadShape。writable 从宿主已验证 capability 派生，不能由 RAG 置信度或 LLM 判断设置。

**分页算法。** 大结果建立 readSession，绑定 source version、查询/选择范围、排序版本、用户工作区与调用目的。cursor 为宿主生成的 opaque token，映射到下一偏移或稳定键位置；不能让模型自由构造 nativeOffset。每页包含 returnedCount、totalCount（已知时）、hasMore、nextCursor、missingFields、truncationReason。会话失效返回 `STALE_READ_CURSOR` 与重新读取入口，不自动换新版本拼接旧页。

按事件/脚本返回正文时，如果字符摘要截断，必须有取得剩余正文的可执行工具参数。没有 continuation 的摘要不得广告为“完整原生读取”。写入 precondition 要求目标所需字段/read coverage 完整；不能只检查 envelope 带一个 sourceHash。

**结构化结果裁剪。** 保留身份和版本，再保留最小显示摘要；剩余 bytes 用于数据。身份本身超过预算时返回 `RESULT_IDENTITY_TOO_LARGE`，不截断 hash/handle。JSON 序列化后检查实际 UTF-8 bytes，超限减少 items 或文本 codepoint 前缀并重新序列化；不能把 JSON 字符串直接 slice。原 `MAX_BOUNDED_TOOL_RESULT_CHARS` 仍可作为额外展示约束，但不叫字节预算。

**写入规则。** native handle 只能由宿主表解析；sourceSha 只作版本检查；mutation 可以产生候选 artifact，但不能越过现有 `editorMutationService`/事务。未支持的地图字符串/TAE 参数项返回该字段的 blockedReason，不调用原始二进制覆写工具绕过。

**测试。** 为 PARAM/FMG/EMEVD/LUABND/MSB/TAE 各准备一份大于单页的数据。通过实际 agentToolBridge 获取全部必需字段，确认连续页无重叠/遗漏且同一 version；第 2 页前变更源，旧 cursor 必须失败；摘要不可变成整文替换；同 ID 不同域不混淆；display_label 修改不得落 native 字节。

## 测试入口与登记

新增薄入口`packages/core/src/testing/runAuditSf07Smoke.ts`。它只按显式`--layer unit|native`调用本任务的真实测试，不是另一个测试框架。unit使用确定fixture调用生产导出；native通过现有`resolveNativeFixture`、`createSmokeWorkspace`和Bridge测试facade调用真实native命令。未知layer退出非零；缺native资源输出skipped，由require-executed判失败。章节提及的具名smoke可作为本入口调用的专项实现，不能再复制同一算法。

core新增脚本（需实施后才存在）：
```json
{
  "test:audit-sf-07-unit": "tsc -b ../shared . && node dist/testing/runAuditSf07Smoke.js --layer unit",
  "test:audit-sf-07-native": "tsc -b ../shared . && node dist/testing/runAuditSf07Smoke.js --layer native"
}
```

根package用同名脚本转发至`@soulforge/core`，tier按脚本后缀登记unit/native。新入口不得用 `assert(true)`、只读源码includes或只跑reference程序。注册后先list，确认包含`tasks/SF-07.required-suites.json`列出的全部项，再执行：
```bash
node scripts/verify.mjs --audit
node scripts/verify.mjs --tier all --filter audit-sf-07 --list
node scripts/verify.mjs --tier all --filter audit-sf-07 --require-executed --no-bail --json-out artifacts/audit-execution/SF-07.json
node scripts/audit-execution/check-verify-summary.mjs artifacts/audit-execution/SF-07.json docs/audit-execution/tasks/SF-07.required-suites.json
npm run typecheck
```

`tools/check-verify-summary.mjs`在SF-00复制/集成到上述新增scripts路径；required-suites文件随执行资料放到上述docs路径。不得在文件还不存在时声称命令可直接运行。

现有core回归另外执行并保留结果；它们不替代新增反例：
```bash
npm run test:native-edit-facade -w @soulforge/core
npm run test:emevd-agent-tools -w @soulforge/core
```

## 通过条件与禁止捷径

关联原报告问题：原报告架构建议/补验范围，未新增确定bug断言。关联验收项：T19, T24, T27, T43。每个T项的原始判据见总表，不能只检查返回ok。

必须记录旧反例的失败、修复后的通过、至少一个无关正常行为不回退以及至少一个非法输入仍拒绝。实际仓库入口缺失、环境缺失、依赖未合并、未知native profile、前端未接线各自标blocker；不能删测试或降级类型消除红色。参考算法通过不等于本任务完成。

输出 `artifacts/audit-execution/SF-07/task-evidence.json`，包含实际commit、changedFiles、productionSymbols、测试结果/哈希、受影响schema、剩余限制。没有运行的层用not_run，不能填写passed。
