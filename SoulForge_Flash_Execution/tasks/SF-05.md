# SF-05执行卡：地图查询、批量目标与快照复用

基准为`f9cb0cd76a11bfa2857b48acbc779bfac0355d96`。本卡是施工任务，当前状态not_started。先读`chapters/00-执行总则.md`与本卡；不可只读标题。依赖：SF-02, SF-03, SF-12。依赖指已合并的接口与必要验证，不是另一个模型说“做了”。

## 允许修改的位置

既有源文件候选白名单如下，必须在SF-00的baseline-map中证明实际存在与锚点一致；不存在不按同名自建。
- `packages/core/src/editing/mapService.ts`
- `packages/shared/src/map-document.ts`
- `packages/core/src/ai/toolRegistry.ts`

新增对象如下；若同功能已有实现，只扩展既有owner，不创建第二套。同功能复用决策写入baseline-map，由集成者固定实际路径。
- `packages/core/src/testing/runAuditSf05Smoke.ts`（新增）
- `packages/core/src/testing/runAuditMapSelectionSmoke.ts`（新增）

共同登记文件仅由集成者修改`package.json`、`packages/core/package.json`、`scripts/verify/tiers.mjs`。允许本任务添加自己的测试helper与fixture描述到`packages/core/src/testing/audit/sf-05/`；这是新增测试目录，不可在其中复制一套生产算法并以此替代生产测试。修改名单之外的native/host入口必须先加入baseline-map得到精确路径，不默认授权整个目录。

需要取证绑定：本卡列出的固定入口。绑定记录包括实际文件、symbol、调用者和版本；不以猜测文件名代替。renderer实现不在本卡权限内。

## 算法与业务步骤

## SF-05：地图查询、批量目标与快照复用

### 1.5.1 组合查询采用交集，不再选择一条分支

修改 `queryMapEntities`。`modelName`、`entityId`、`kind`、`nameContains` 都是可选的 AND 条件。建立候选集时可选成本最小的索引集合，最终仍执行全部给定谓词。大小写折叠只用于 nameContains 搜索；identity/modelName 的精确匹配沿既有 canonical 规则，不能同时改变底层资源名。

`regionName` 表示“引用该 Region 的事件”，不是模糊字符串过滤。先用 native 身份解析 Region；名称歧义则返回候选；引用图 coverage 不完整时返回 `MAP_REFERENCE_COVERAGE_INCOMPLETE`，不返回空集。取得事件集合后继续与其他条件求交。冲突条件得到合法空集及完整 coverage，不忽略其中一个条件。

### 1.5.2 批量操作的目标集合

`batchTransformMapParts` 删除 `if (!part) continue`。第一遍解析全部 requested targets 到 canonical handles；任一缺失或歧义返回错误，staging 调用次数必须为零。两个输入别名解析到同一个 handle 时返回 `MAP_DUPLICATE_TARGET`，不重复计算 delta，也不静默去重后继续把 requestedCount 当 modifiedCount。

第二遍计算目标值。positionDelta 与原 position 相加；scaleMultiplier 与原 Part scale 相乘；输入 rotation delta 的意义固定为当前接口原生 Euler 分量增量，不能暗中升级成世界轴旋转。需要世界/局部矩阵旋转的请求走 SF-25 新协议。浮点量化采用格式写入结果；输出包含 requested/resolved/effective counts，零有效变化返回 no-op。

默认不提供 bestEffort 参数。用户将来需要部分成功时另建显式批次模式，返回每个目标状态；不能借这次修复把普通 ok:true 继续用在部分成功上。

### 1.5.3 消除重复完整加载

现有入口 `loadMapDocument`、`executeMapTransaction`、`batchTransformMapParts` 保留。新增内部 `MapSnapshotLease`，包含 doc、sceneGraph、sourceIdentity、sourceVersion、released 标记。它由宿主创建，不允许模型 JSON 伪造。

`executeMapTransaction` 增加内部可选 snapshot 参数：传入时验证 file 与 workspaceEpoch 一致，并在提交前检查 sourceHash；没有传入时调用一次 loadMapDocument。批量入口传入刚读取的 lease。事务内完成一次提交后权威重读，返回 `verifiedPostState`，包含触及对象及完整 sourceVersion。外层 afterList 从该结果生成，删除为了生成 afterList 的第四次 loadMapDocument。

必要的 native 提交后重读不能被缓存替代。该重读必须绕过以旧 revision 为键的投影，读到真实新 outer/payload hash。可以复用新解析结果在 Core/main 之间传递，不再重复 IO/JSON/sceneGraph 构造。

### 1.5.4 验证与前端合同

新增 `runAuditMapSelectionSmoke.ts`，使用实际 `queryMapEntities` / `batchTransformMapParts`。测试 modelName 匹配两项但 entityId 只匹配一项；一个有效目标加一个不存在目标；两个别名指向同一目标；读后源文件变化；无效 float；量化 no-op；外层 afterList 不触发额外 load。

mock 调用次数只能验证函数编排，另需真实 `runBridge` trace 的 sourceHash、command、responseBytes、parse/projection 计数。UI 接收 `verifiedPostState` 时以同一 revision 更新已打开对象；前端不自行再读“某个同名对象”替代返回的 handle。SF-29 验证用户看到的更新。

## 测试入口与登记

新增薄入口`packages/core/src/testing/runAuditSf05Smoke.ts`。它只按显式`--layer unit|native`调用本任务的真实测试，不是另一个测试框架。unit使用确定fixture调用生产导出；native通过现有`resolveNativeFixture`、`createSmokeWorkspace`和Bridge测试facade调用真实native命令。未知layer退出非零；缺native资源输出skipped，由require-executed判失败。章节提及的具名smoke可作为本入口调用的专项实现，不能再复制同一算法。

core新增脚本（需实施后才存在）：
```json
{
  "test:audit-sf-05-unit": "tsc -b ../shared . && node dist/testing/runAuditSf05Smoke.js --layer unit",
  "test:audit-sf-05-native": "tsc -b ../shared . && node dist/testing/runAuditSf05Smoke.js --layer native"
}
```

根package用同名脚本转发至`@soulforge/core`，tier按脚本后缀登记unit/native。新入口不得用 `assert(true)`、只读源码includes或只跑reference程序。注册后先list，确认包含`tasks/SF-05.required-suites.json`列出的全部项，再执行：
```bash
node scripts/verify.mjs --audit
node scripts/verify.mjs --tier all --filter audit-sf-05 --list
node scripts/verify.mjs --tier all --filter audit-sf-05 --require-executed --no-bail --json-out artifacts/audit-execution/SF-05.json
node scripts/audit-execution/check-verify-summary.mjs artifacts/audit-execution/SF-05.json docs/audit-execution/tasks/SF-05.required-suites.json
npm run typecheck
```

`tools/check-verify-summary.mjs`在SF-00复制/集成到上述新增scripts路径；required-suites文件随执行资料放到上述docs路径。不得在文件还不存在时声称命令可直接运行。

现有core回归另外执行并保留结果；它们不替代新增反例：
```bash
npm run test:map-transaction-atomic -w @soulforge/core
npm run test:map-document-scale -w @soulforge/core
```

## 通过条件与禁止捷径

关联原报告问题：F04, F05, F13。关联验收项：T07, T08, T09。每个T项的原始判据见总表，不能只检查返回ok。

必须记录旧反例的失败、修复后的通过、至少一个无关正常行为不回退以及至少一个非法输入仍拒绝。实际仓库入口缺失、环境缺失、依赖未合并、未知native profile、前端未接线各自标blocker；不能删测试或降级类型消除红色。参考算法通过不等于本任务完成。

输出 `artifacts/audit-execution/SF-05/task-evidence.json`，包含实际commit、changedFiles、productionSymbols、测试结果/哈希、受影响schema、剩余限制。没有运行的层用not_run，不能填写passed。
