# SoulForge V0.5 mission1 收敛台账

本台账保留在工作区，因为仍有明确的产品能力与治理阻塞；未执行 commit、push 或 evidence seal。

## 基线

- Branch: `main`
- HEAD: `a5ed31bb42736c68a5e8f9c937cf5d1c276b11f8`
- 工作树：`PARTIAL`。当前治理快照为 95 个未提交文件；开始时已有用户/其他会话未提交改动，本轮未重置、清理或覆盖。
- `git diff --check`：`PASS`。仅有 Git 的 LF/CRLF 转换提示，无 whitespace 错误。

## AGENT / RAG

| 项目 | 状态 | 证据与边界 |
| --- | --- | --- |
| AGENT-01 Semantic Resolver | `PASS` | `packages/core/src/semantic/resolver.ts`；canonical graph、exact address、ambiguous 与 coverage 状态均有 smoke。 |
| AGENT-02 Canonical Entity Graph | `PASS` | 节点/边含 identity、source URI、revision、provenance、evidence 与 epistemic state。 |
| AGENT-03 Coverage Semantics | `PASS` | zero hit 不再自动等于不存在；partial/not-indexed/stale/source unavailable 有结构化结果。 |
| AGENT-04 Task Model | `PASS` | `taskModel.ts` 生成 inspect/diagnose/modify/create 分类、目标、约束、后置条件与阶段子目标链。 |
| AGENT-05 Completion Contract | `PASS` | session host 只接受 typed completion evidence；缺证据的提前结束会变成 incomplete/partial。 |
| AGENT-06 Progress / No-progress | `PASS` | information gain 指纹、重复查询熔断与 replan smoke 通过。 |
| AGENT-07 Epistemic State | `PASS` | observed/derived/hypothesized/unverified 类型与 provenance 贯通 resolver 结果。 |
| AGENT-08 Semantic ChangeSet | `PASS` | 依赖顺序、目标 revision、collision-aware reservation 与 plan validation 有实现和 smoke。 |
| AGENT-09 Workspace Atomic Transaction | `PASS` | `commit_semantic_change_set` 已接入生产 Patch Engine boundary；跨域计划统一 stage、validate、staged reread、commit、committed reread、postcondition 与失败回滚。 |
| AGENT-10 Query Planning | `PASS` | 每个子目标有 QueryPlan、候选工具、查询历史、结果和未决问题。 |
| AGENT-11 Ephemeral Context | `PASS` | durable history 与 broker/RAG ephemeral 注入分离；重试/压缩不会污染任务查询。 |
| AGENT-12 RAG Revision | `PASS` | chunk/reference/embedding 带 source revision；旧 revision 不参与检索；Context Broker 按 authority/revision/相关性排序并按 UTF-8 预算截断。 |
| AGENT-13 Memory | `PASS` | memory store、AI log 与 session host 接线通过 conformance。 |
| AGENT-14 Resume Prompt Version | `PASS` | resume 使用当前 system policy，并记录 prompt/hash/runtime metadata。 |
| AGENT-15 Observability | `PASS` | rollout、provider usage、tool registry、resolver、RAG、workspace revision 与 completion metadata 有 durable 记录。 |
| AGENT-16 Provider Capability | `PASS` | `providerCapabilities.ts` 与三协议 production factory/adapter 已验证 request > explicit config 优先级；未声明的 tools/vision/reasoning/采样字段 fail-closed。fake loop、66 项 sampling 与 63/63 conformance 通过；不宣称真实 upstream 可用，也不伪造自动 probe。 |
| Dirty canonical precedence | `PASS` | `apps/desktop/src/main/ipc.ts` 的生产 EditorDocumentStore 入口在 FMG/PARAM/EMEVD/MSB canonical read 后注册 dirty projection，提交后按 revision 清理并重读；WorkspaceIndex/resolver smoke 通过。未知格式仍由 capability fail-closed。 |
| Create safety | `PASS`（有界） | template-backed MSB Part duplicate/create 已进入统一 transaction；非 Part、空白 subtype、缺 template 或 malformed Blender delta 仍结构化返回 `MAP_CREATE_UNSUPPORTED` / `MAP_DUPLICATE_UNSUPPORTED`，不伪装成通用创建。 |
| Multi-domain production wiring | `PASS` | `commit_semantic_change_set` 已使用生产 `executeSemanticPatchProposalTransaction`，多个 domain proposal 进入同一 Patch Engine transaction，并有 staged/committed hash、index/reference/RAG 刷新与失败回滚；独立 human editor 保存路径不冒充 Agent multi-domain。 |

## MAP

- `PASS`：mapId/baseRevision 校验、全量 preflight、ordered working state、单事务 staging/commit、batch delete、typed property/model/transform、authoritative reread 与失败回滚。
- `PASS`：真实 MSB m10/m11/m11_01/m11_02/m13/m15/m17/m20/m25 全量读取通过；MSB writer 真实 m11 的 25 项 mutation、未注册类型拒绝、重读和无残留通过。
- `PASS`：10k MapDocument/SceneGraph 与 Blender 导入导出规模 smoke 通过；Blender 变更仍汇入同一 transaction boundary。
- `PASS`（有界 native contract）：真实 m11 template-backed Part duplicate/create 已由 Bridge 复制完整原生 entry、重建 PARTS offset table、保留 entry prefix，并在 reread 中验证类型、模型、transform、entityId 与真实 stable identity；单 transaction ordered mutation 也已验证。
- `PASS`（统一入口）：`execute_map_transaction` Agent tool、Human `batchTransformMapParts`、Blender `import_map_from_blender` 均在真实 m11 临时 overlay 经 Patch Engine 写入，再从 canonical MSB reread；`test:map-native-convergence` 三条 wrapper 全部通过。MSB file revision 与 Bridge payload `nativeDocumentHash` 已分离，避免压缩文件 hash 错配。
- `PARTIAL`（明确边界）：当前 create/duplicate 只支持 template-backed Part；opaque subtype/reference/group bytes 是原样复制并受 native reread 保护，但 Bridge 尚未独立语义解析所有内层 payload。blank create、Region/Event/Model 创建及任意未知 subtype 继续 fail-closed，不能宣称完整 MSBS 创建能力。

## EVENT

- `PASS`：DarkScript/EMEDF 编译、X 参数 source/target offset、alignment、insert/delete remap、stable identity、结构化失败与 native writer reread smoke 通过。
- `PASS`：真实 `common.emevd.dcx` 读取 156 events / 4618 instructions，真实 EMEDF 405 instructions / 27 banks，EMEDF decode/mutation 与 event add/delete GC 通过，authority 为 `native-verified`。
- `PASS`：真实 DFLT corpus 的 multi-corpus 矩阵通过，覆盖 7 个 DFLT corpus 的 kind/instance；251 文件 BND/脚本 native writeback 全部 reread、未触碰原件且 rollback byte-identical。
- `PASS`：真实 KRAK corpus 通过 Bridge/Oodle 的完整 multi-corpus typed matrix；每个 KRAK 样本均在临时 overlay 完成 native 读取、EMEDF typed mutation、Patch commit、byte-consistent 与 semantic reread，结果 `skips: []`。这证明生产 Bridge/Oodle 矩阵，不宣称游戏内加载完成。

## ACTION

- `PASS`：HKX bounds-check、mask/scalar/quaternion/decompression、reference pose、binding/companion identity、FLVER mapping 与 strict fail-closed 路径已接入；无 `animations[0]`/`bindings[0]`/`skeletons[0]` 猜测。
- `PASS`：真实 `chr/c0000.anibnd.dcx` SplineCompressed differential 通过；21 frames、146 HKX bones、112 transform tracks、73 moving bones；C#/TS translation/rotation/scale 误差在 smoke 阈值内，FLVER mapping 非 identity。
- `PASS`：AnimationPlaybackClock、timeline、Three WebGPU-first/WebGL2 fallback headless 功能与资源释放 smoke 通过。
- `PARTIAL`：真实 TAE 读取 939 animations / 23420 events / 20696 groups；本机模板布局下 21391/23420 个观测事件完成字段解码，未知/不匹配类型保留原始 hex。`hkaDefaultAnimatedReferenceFrame` 的 frame type、up/forward、duration 与逐帧 raw XYZ/W 已由 Bridge 读取并进入 clip/pose 响应。
- `PARTIAL`：逐 identity 的真实 `read-tae-animation-clip` coverage 统计为 `payload=836`、`unsupported=27`、`failed=76`；836 个成功 payload 全部为 `tagfile + SplineCompressed`，未在本机 c0000 corpus 观察到 `Interleaved`。27 个 additive clip 以 `ACTION_ADDITIVE_UNSUPPORTED` 结构化拒绝，76 个失败保留 companion HKX 缺失/导入诊断；没有将这些结果提升为完成能力。
- `PASS`（边界内）：836 个 payload 均通过 frame/duration、track binding、reference pose、实际 spline payload 合同检查；其中 382 个含 root-motion payload，并完成 t0/中点 raw、translation、rotationAngle 有限性采样；802 个 payload 在 t0/中点存在移动骨骼。Bridge 的 Havoc TAG0 读取通过 `ActionHkxReadGate` 串行化，避免 daemon 并发请求破坏内部集合。
- `PASS`（独立 differential）：真实 `chr/c0000.anibnd.dcx` 的 C#/TS pose differential 通过；不宣称真实 `Interleaved`、packfile corpus 或 FLVER mesh skin deformation 完成。TAE writer 当前 fixture authority 为 `candidate`，不提升 native authority。

## Verification Matrix

| 命令 | 状态 | 结果 |
| --- | --- | --- |
| `git diff --check` | `PASS` | 无 whitespace 错误；仅换行提示。 |
| `npm run typecheck` | `PASS` | shared/core/desktop typecheck 通过。 |
| `npm run bridge:build` | `PASS` | 0 errors；保留 57 条既有编译 warning，未把 warning 误报为零。 |
| `npm run build` | `PASS` | shared/core/desktop、renderer 与 release manifest 构建通过；manifest authority 仍为 partial。 |
| `npm run test:ai-fake-loop` | `PASS` | 双协议 fake server tool loop 通过。 |
| `npm run test:ai-conformance` | `PASS` | 63/63。 |
| `npm run test:semantic-convergence` | `PASS` | TaskModel/Resolver/Coverage/No-progress/ChangeSet/Completion、生产 `commit_semantic_change_set` 跨域事务与 rollback smoke 通过。 |
| `npm run test:rag` | `PASS` | workspace retrieve/embedding smoke 通过，synthetic/mock 不提升 native authority。 |
| `npm run test:emevd-dark-script-compiler` | `PASS` | 编译与诊断 smoke 通过。 |
| `npm run test:emevd-instruction-structural` | `PASS` | 指令/事件增删 smoke 通过。 |
| `npm run test:emevd-plan-commit` | `PASS` | 13/13，authority 上限 partial。 |
| `npm run test:emevd-multi-corpus-matrix` | `PASS` | 配置真实 game root；DFLT 与 KRAK 均经 Bridge/Oodle 完整 typed matrix、Patch commit、byte-consistent/semantic reread，`skips: []`。 |
| `npm run bridge:verify:emevd` | `PASS` | 配置真实 game root；native-verified。 |
| `npm run test:native-corpus-writeback` | `PASS` | 251 files、84 writable BND4 写回全部重读与 rollback 验证。 |
| `npm run bridge:verify:msb` | `PASS` | 真实 m10 native-verified。 |
| `npm run bridge:verify:msb-writer` | `PASS` | 真实 m11 native-verified。 |
| `npm run bridge:verify:msb-all` | `PASS` | 9 maps native-verified。 |
| `npm run bridge:publish` | `PASS` | Release self-contained publish 已重建；真实 writer smoke 使用 production candidate。 |
| `npm run test:map-transaction-atomic` | `PASS` | 7 cases：stale revision、preflight、ordered duplicate/create、多 delete、单次 commit 与 postcondition。 |
| `npm run test:map-native-convergence` | `PASS` | 显式真实 game root；Agent/Human/Blender 三条 wrapper 在 m11 临时 overlay 经 Patch Engine commit 后 reread。 |
| `npm run test:native-edit-facade` | `PASS` | 20 项 facade/permission/tool registration 检查通过。 |
| `npm run test:map-document-scale` | `PASS` | 10k scale smoke 通过。 |
| `npm run bridge:verify:tae` | `PASS` | 真实 c0000 读取通过；模板布局加载并解码 21391/23420 个观测事件，未知类型保留 raw hex，authority=partial。 |
| `npm run test:action-corpus-coverage` | `PARTIAL` | 真实 c0000 逐 identity 939 项 coverage：836 payload、27 additive unsupported、76 structured failed；382 root-motion payload 完成 t0/中点采样，802 moving-bone samples；命令退出 0 仅表示 coverage 报告生成成功，不提升 authority。 |
| `npm run bridge:verify:tae-writer` | `PASS` | synthetic fixture，authority=candidate。 |
| `npm run test:animation-playback-clock` | `PASS` | 播放时钟、timeline、sampler smoke 通过。 |
| `npm run test:action-deterministic-seek` | `PASS` | 反向/多次 seek 与帧边界确定性通过。 |
| `npm run test:three-scene-functional` | `PASS` | headless functional smoke 通过，无真实 GPU/mesh claim。 |
| `npm run test:action-native-differential` | `PASS` | 真实 c0000 C#/TS differential 通过。 |
| `npm test` | `PASS` | core 全套、桌面安全/本地化、SQLite utility 与构建通过。 |
| `npm run verify` | `FAIL` | `test:gov-cli` 的 `cli/seal-success-path-reachable` 因 `SEAL_POSTCHECK_FAILED` / `GATE_EVIDENCE_STALE REL-SCOPE` 失败；其余 88 条按 bail 记为未执行。 |
| `npm run verify:all` | `FAIL` | 同一治理 stale evidence 阻塞，未伪装成全量通过。 |
| `node scripts/gov.mjs status` | `BLOCKED` | 当前快照 95 个未提交文件；REL-SCOPE/REL-A/REL-E/REL-G/REL-I stale evidence、`SCOPE-SEKIRO-BUILD` freeze violation、旧 `W-REL-D-GAMELOAD-01` claim。 |

## Remaining Gaps

1. provider 的生产能力来自显式 config；未知 provider 不自动探测或持久化 upstream 能力，避免把 HTTP 400 当 capability negotiation 成功。
2. MSB duplicate/create 目前只达到真实 template-backed Part 有界 authority：完整原生 entry 与 opaque payload/reference/group bytes 会随模板复制并经 reread 验证，但内层 payload 尚未全部语义解析；blank create 与非 Part subtype 仍 unsupported。
3. `executeSemanticWorkspaceTransaction` 是跨域协调与 smoke 的统一入口，但现有各个 human editor writer 仍保留自己的 domain transaction；不能把它们写成全部已迁移到同一个入口。
4. TAE 未覆盖类型的字段语义、真实 Interleaved corpus、packfile corpus 与真实 mesh skin 证据仍不完整；root-motion 已有 382 个真实 payload 的合同与采样证据，但尚无游戏内/mesh 视觉验证。
5. release 仍被治理层阻塞：需要用户授权的 scope/unfreeze 裁定与工程侧重新 seal stale evidence；本轮没有擅自释放 stale claim、修改冻结裁定或写入 evidence。

因此本轮状态是“代码与可运行验证已收敛到上述边界”，不是无条件 release PASS；台账按要求保留，直到这些 gap 或治理裁定被合法处理。
